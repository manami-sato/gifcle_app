doctype html
html(lang="ja")
	head
		include _head.pug
		link(rel="stylesheet", href="css/sign.css")
	body
		main
			div.sign__wrap
				div.sign__wrap--headline 新規会員登録
				form
					div.sign__form#item1
						p.sign__form--headline メールアドレス
						input(type="text",placeholder="example@example.com").sign__form--value
					div.sign__form#item2
						p.sign__form--headline パスワード
						input(type="password",placeholder="半角英数字で8文字以上").sign__form--value
					div.sign__form#item3
						p.sign__form--headline パスワード（確認用）
						input(type="password",placeholder="半角英数字で8文字以上").sign__form--value
					div.sign__form#item4
						p.sign__form--headline 誕生日（変更できません）
						div.sign__form--value
							select(name="year").yearBox
								option(value="",selected) -
							span 年
							select(name="month").monthBox
								option(value="",selected) -
							span 月
							select(name="date").dateBox
								option(value="",selected) -
							span 日
					a(href="calendar.html").sign__form--btn 登録
				span.sign__wrap--spl すでにアカウントをお持ちの方は
					a(href="signin.html") ログイン
		script(src="js/common.js")
		script(src="js/signup.js")